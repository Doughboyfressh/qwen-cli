openai
rich
duckduckgo-search
pyreadline3; sys_platform == "win32"
tomli; python_version < "3.11"
prompt_toolkit
youtube-transcript-api
trafilatura
pypdf
# HTML extraction quality (graceful fallback to regex if absent)
lxml
lxml-html-clean
readability-lxml
# LSP integration (language server protocol)
multilspy
python-lsp-server[all]
jedi
# Process liveness checks for team_spawn_agent's stale-agent detection
# (already pulled in transitively by multilspy; declared explicitly since
# we now import it directly)
psutil
# Clipboard support for /copy and /paste
pyperclip
